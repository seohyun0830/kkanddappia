import pygame
from .images import *

'''
def f_guide(window):
    clock = pygame.time.Clock()
    pygame.event.clear()
    

    play = True
    while play:
        clock.tick(60)

        for event in pygame.event.get():        
            if event.type == pygame.QUIT:
                return -1
            
            if event.type == pygame.MOUSEBUTTONDOWN:
                idx += 1
        
        text = font.render(texts[idx], True, (255, 255, 255))
        
        window.blit(text, pos[idx])
        pygame.display.update()

def g_move(window):
    font = pygame.font.Font("DungGeunMO.ttf", 40)
    texts = ["→", "←", "↑", "↓","이동"]
    pos = []
    # 변수 초기화
    idx = 0


def g_tab(window):
    pass

def g_ladder(window):
    pass
    

'''